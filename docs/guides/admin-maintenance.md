# Админ-API: бэкап, SSH, баланс провайдеров

Заметки по эксплуатации админских эндпоинтов (`/api/v1/admin/models/*`), включая
разбор пунктов внешнего аудита от 2026-09-12/13 (проверено кодом и на стенде 18).

## Бэкап настроек: категории берутся из БД

`GET /api/v1/admin/models/backup` — категории читаются динамически
(`SELECT DISTINCT category FROM system_configs`), как в `/backup-documents`.

Было: список `BACKUP_NAMESPACES` захардкожен и отстал. В бэкап не попадали
`providers` (провайдеры LLM!), `search` (режим поиска), `system` (блокировки
загрузки и обработки), `setup`, `upload_config`, `entity_cache` — при переносе на
другой сервер конфигурация провайдеров терялась.

Сейчас: в бэкапе 9 категорий (проверено) —

```
function_map, kg_config, process_logs, providers, search, setup,
system, upload_config, web_monitor
```

Кэши по умолчанию исключаются (`entity_cache` — эмбеддинги сущностей,
восстанавливать бессмысленно), но включаются параметром `?include_caches=true`.
В ответе есть `categories` и `skipped_caches` — видно, что именно уехало.

Восстановление (`POST /backup-restore`, multipart, поле `file`) формат не меняло:
круг «скачал → залил» проверен на стенде — `restored: 9, errors: []`, значения
`providers`/`search`/`system` после восстановления не изменились.

## SSH: пароли не в командной строке

Было (`restart-ollama` и `ssh_manager.test_connection`): пароль подставлялся
аргументом sshpass (флаг `-p`) → виден в `ps` на хосте; sudo-пароль уходил в
командной строке на удалённой стороне; вызов шёл через `shell=True`.

Стало:

- SSH-пароль — через переменную окружения: `sshpass -e`, `SSHPASS` в `env`
  дочернего процесса (в argv пароля нет);
- sudo-пароль — на stdin (`sudo -S -p ''`), поэтому в `ps` удалённой стороны его
  тоже нет;
- вызовы только списком аргументов, `shell=True` не используется;
- имя systemd-сервиса санитизируется (`_safe_service_name`: только
  `[A-Za-z0-9_.@-]`, иначе `ollama`) — защита от инъекции через настройку.

Тест: `tests/test_admin_audit_fixes.py` — пароль не в argv, `SSHPASS` в env,
sudo-пароль в stdin, инъекции в имени сервиса отсекаются, `shell=True` и
`sshpass -p` в исходниках отсутствуют.

## Баланс провайдеров

Три дублирующих эндпоинта сведены к одной функции `_provider_balance()`:
`/ext-llm/balance` и `/graph/balance` (старые конфигурации) и
`/providers/{id}/balance` (актуальный, его зовёт админка).

DeepSeek: ответ `{is_available, balance_infos: [{currency, total_balance,
granted_balance, topped_up_balance}]}`. Раньше в двух местах из трёх читалось
поле `balance`, которого нет → в интерфейсе было «0.0 токенов» при живом ключе.
Теперь баланс берётся из `balance_infos`, при отрицательной сумме статус
`balance_ok=false` и приписка «(баланс исчерпан)» (у DeepSeek `is_available`
остаётся `true` и при исчерпанном балансе — проверено на живом ключе: −1.18 CNY).

Провайдеры, которые баланс не отдают (gigachat, openai): `balance_ok=null`,
`balance_known=false`, в админке нейтральное «—», а не красная ошибка и не нули.
Ошибки HTTP различаются: 401/403 → «API ключ недействителен», иначе `HTTP <код>`.

## Бэкап с документами: длинные имена файлов (2026-09-13)

`GET /backup-documents` вообще не формировался: отдавал 200 и JSON
`{"status":"error","message":"Ошибка формирования ZIP: [Errno 36] File name too
long: '/app/data/uploads/0e33936d-..._Использование алгоритмов ГОСТ ...'"}`.

Причина: имя собиралось как `f"{doc_id}_{filename}"`, а `filename` в БД — полное
исходное название (до 205 символов). В сумме больше 255 байт (лимит имени в
ext4), и уже `path.exists()` бросал `ENAMETOOLONG` — `pathlib` эту ошибку не
проглатывает (в отличие от `os.path.exists`).

Теперь:

- файлы ищутся по индексу каталога `uploads` (один проход, ключ — `document_id`,
  первые 36 символов до «_») — длинные имена не собираются вообще;
- имя внутри архива обрезается до безопасной длины с сохранением расширения;
- ошибка на отдельном файле не убивает архив: пропуски перечислены в
  `backup_warnings.json` внутри самого архива;
- неудачный ZIP отвечает 500 (раньше браузер сохранял 494-байтный JSON как «zip»).

Проверено на стенде: архив собирается — 51 запись (47 файлов документов +
`documents_meta.json`, `aliases.json`, `config_store.json`, `chat_history.json`),
пропущенных файлов нет, `config_store.json` содержит все категории (в отличие от
`/backup`, здесь кэш `entity_cache` тоже попадает — это полный бэкап).

## restart-ollama: контракт ответа

Страница админки проверяет `result.status` (`success` / `warning`, иначе —
ошибка). При переписывании эндпоинта это поле было потеряно, страница показывала
ошибку при успешном перезапуске. Сейчас возвращаются и старые имена полей
(`systemctl_active`, `http_responding`), и новые (`service_active`,
`api_responding`). Пауза 8 секунд заменена опросом API до ~20 с с выходом сразу
после ответа. Второй SSH-вызов использует `sudo -S` с паролем по stdin: `sudo -n`
без tty ненадёжен (кэш прав может не сработать), из-за чего живой сервис
показывался как неактивный.

## Третья проверка кода (2026-09-13)

Исправлено:

- **Баг в моей же правке (критично)**: `test_ext_llm` вызывал `async def
  get_ext_llm()` без `await` — корутина создавалась и выбрасывалась
  (`RuntimeWarning: coroutine was never awaited`), настройки не перечитывались.
  Введены синхронные загрузчики `_load_ext_llm_from_db()` и
  `_load_graph_model_from_db()`; все читатели (тест, список моделей, `/ext-llm`,
  `/graph`, оба balance) работают через них, глобалы остались только fallback.
- **Импорт `config_store`**: перенесён в шапку модуля, 38 локальных копий убраны
  (остался только явный алиас `import config_store as cs`).
- **`/deploy`**: пути берутся из `get_settings()` (`DEPLOY_SRC_PATH`,
  `DEPLOY_REPO_PATH`, `ENV_FILE_PATH`), whitelist расширений, `base64` со
  `validate=True`, лимит размера `DEPLOY_MAX_FILE_BYTES`, и — важное — `.py`
  перед записью компилируется: синтаксически ломаный файл не записывается.
- **`/ext-llm`** больше не отдаёт API-ключ открытым текстом (маска + `api_key_set`).
- **Синхронные вызовы**: в `asyncio.to_thread` добавлены Neo4j-вызовы элиасов,
  индексация `uploads`, запись файлов в ZIP, чтение документов и циклы
  `config_store` в `/backup` и `/backup-restore`. Всего в файле 42 обёртки.
- **Кэши в `/backup-documents`**: исключаются как и в `/backup`, включаются
  `?include_caches=true`; в лог добавлен размер архива в МБ.
- **`restart-ollama`**: опрос API после перезапуска сокращён до ~12 с.
- **`provider_service.load_default_prompt`** — публичный (приватный вызов из
  роутера убран).
- **Контейнеры keycloak/neo4j** ищутся по compose-метке, с fallback на имя.

Проверено на стенде:

- `scripts/check_admin_routes.py` — сплошная проверка **всех 110 админ-роутов**:
  без токена все отвечают 401/403, не-администратор проходит только на
  `GET /admin/models/upload-config` (штатное исключение для страницы «Документы»),
  временный пользователь создаётся и удаляется. OpenAPI на стенде отключён
  (`/api/openapi.json` → 404), поэтому список роутов берётся из `app.routes`.
- `/deploy`: `.so` — отказ; битый base64 — отказ; ломаный `.py` — отказ
  («Файл .py не компилируется (строка 1): invalid syntax»); валидный `.py` и
  валидный base64 — запись; неизвестная кодировка — отказ.
- Нагрузка: 10 параллельных `/aliases` (Neo4j) при одновременном `/health` —
  health отвечает за 230 мс, ошибок нет: цикл не блокируется.
- `restart-ollama` отдаёт контракт (`status`/`systemctl_active`/`http_responding`
  + новые поля), но перезапуск не подтверждается: «Permission denied» к хосту
  `192.168.50.41`. Это не код — в настройках SSH админки лежит недействующий
  доступ к Ollama-хосту; поправить в админке.

Известное ограничение: `/deploy` не умеет записывать пустой файл (`file_content`
считается обязательным) — при необходимости снять проверку на пустую строку.

Отложено осознанно (шаг 10 аудита): замена `data: dict` на Pydantic-модели в девяти
эндпоинтах. Причина: интерфейс сохраняет конфиги частичными словарями, и переход
на модели с `exclude_unset` меняет семантику записи (можно затереть поля, которые
UI не отправляет). Делать это нужно по одному эндпоинту вместе с проверкой
соответствующей страницы, иначе есть риск тихой потери настроек.

## Третья проверка: что подтвердилось и что осталось

## Вторая проверка кода: что подтвердилось, что опровергнуто (2026-09-13)

Подтверждено и исправлено:

- **Дубли имён функций**: блокировка ингеста имела те же имена, что форматы
  загрузки (`get_upload_config`/`save_upload_config`) — переименованы в
  `get_ingest_config`/`save_ingest_config`; роуты не менялись.
- **`test_ext_llm` читал глобал** `_ext_llm_config` — при нескольких воркерах
  uvicorn глобал расходится с БД, тест мог идти по старым данным. Теперь настройки
  перечитываются из `config_store` перед проверкой.
- **Синхронные вызовы в async-эндпоинтах**: обёрнуты в `asyncio.to_thread`
  (было 8 вызовов, стало 28) — `docker_monitor` restart/stop/start,
  `system_monitor` cpu/memory/disk/network, все шесть `qdrant_monitor`,
  `ssh_manager.test_connection`, `subprocess.run` в `/deploy` (git pull с таймаутом
  60 с и рестарт compose с 120 с блокировали event loop), а также docker SDK в
  `/system-config`, `/scaling` и `/worker-resources` (вынесены в синхронные
  хелперы `_docker_keycloak_status`, `_docker_worker_state`,
  `_docker_worker_resources`).

Опровергнуто живой проверкой (повторялось в двух аудитах — оба раза неверно):

- **«`get_llm_config` падает с NameError»** — нет: `config_store` импортируется на
  уровне модуля, вызов даёт `HTTP 200` с настройками. Требование к ревьюеру:
  смотреть не только тела функций, но и импорты внизу файла.
- **«Нет авторизации на админские роуты»** — нет: без токена `/backup`, `/deploy`,
  `/ssh-config` дают `401`; временный не-админ получает `403 ACCESS_DENIED`
  («Недостаточно прав»), проверено на стенде (тестовый пользователь создан и
  удалён). Защита — middleware `security.py`, `ADMIN_PREFIXES = {"/api/v1/admin"}`,
  роли `admin`/`kag-admin`.
- **«Дублирование `SELECT DISTINCT category`»** — уже вынесено в
  `_all_config_categories()`; в файле одно вхождение запроса.

Остаётся осознанным (не баг): `/deploy` умеет писать файлы в `/app/src`, делать
`git pull` и перезапускать контейнеры — это RCE для администратора по замыслу
(нужен для дежурного деплоя), доступ закрыт админской ролью. Если такой
функционал не нужен на поставке — эндпоинт стоит удалить или ограничить
локальным доступом; решение за владельцем продукта.

## Что из аудита подтвердилось, но осталось в бэклоге


- `admin_models.py` — 2542 строки, дубли имён `get_upload_config`/
  `save_upload_config` (форматы загрузки и блокировка ингеста), 9 тел `dict`
  вместо Pydantic, 0 `response_model`, хардкод путей (`/app/src`,
  `/home/yartsevn/kag-system`, `/app/kag.env`) и имени контейнера `kag-keycloak`,
  глобалы `_ext_llm_config`/`_graph_model_config`, `asyncio.to_thread` применён
  непоследовательно (8 обёрнутых против 16 прямых синхронных вызовов).
- `/deploy` (запись файлов в `/app/src`, `git pull`, рестарт) — RCE для
  администратора по замыслу; авторизация есть (middleware защищает
  `/api/v1/admin`, роли admin/kag-admin).
- Отклонено как неверное в аудите: «NameError в get_llm_config» (модульный импорт
  `config_store` есть, к моменту запроса модуль импортирован), «нет проверки прав
  на админ-роуты», «`_graph_model_config` используется до определения».

## Контрольный прогон после правок (образ 2026.09.13.8)

Все семь пунктов третьего разбора проверены на стенде повторно:

| Шаг | Что проверялось | Результат |
|-----|-----------------|-----------|
| 1 | `/llm-config` (NameError?) | HTTP 200 с настройками — импорт в шапке модуля |
| 2 | 110 админ-роутов без токена | все 401/403; не-админ проходит только на `GET /admin/models/upload-config` |
| 2 | ключ в `/ext-llm` | `api_key: "***"`, `api_key_set: false` — открытого ключа нет |
| 3 | кэши в `/backup-documents` | по умолчанию 25.0 МБ, 9 категорий, `entity_cache` исключён; с `?include_caches=true` — 26.2 МБ, 10 категорий |
| 4 | `/restart-ollama` | HTTP 200 за 12.7 с (опрос ≤12 с), `status=warning`, поля `systemctl_active`/`http_responding` на месте; причина — «Permission denied» к хосту 192.168.50.41 |
| 5 | `/deploy` | `nope` → отказ; `.so` → отказ; битый base64 → отказ («Only base64 data is allowed»); ломаный `.py` → отказ |
| 6 | нагрузка | health 128–140 мс при 10 параллельных `/aliases`, ошибок 0 — цикл не блокируется |
| 7 | логи api | ни одного `never awaited` / `NameError` за сессию |

Обёрток `asyncio.to_thread` в `admin_models.py` — 43 (включая правку `.env` в
`save_function_map`, Neo4j-вызовы элиасов и оба бэкапа).

Остаётся за владельцем:

- **SSH-доступ к Ollama-хосту** (192.168.50.41) в админке недействителен —
  `/restart-ollama` честно отдаёт `warning` и «Permission denied». Это настройка
  стенда, не код.
- **Решение по `/deploy`**: оставить (удобный дежурный деплой, доступ только у
  admin), ограничить localhost или удалить. Сейчас он защищён ролью и валидацией,
  но по-прежнему позволяет записывать файлы в `/app/src` и перезапускать api.
- **Шаг 10 третьего разбора** (Pydantic вместо `data: dict` в 9 эндпоинтах) —
  отложен осознанно: UI сохраняет конфиги частичными словарями.

## Четвёртый разбор кода (2026-09-13, образ 2026.09.13.10)

Все 15 замечаний подтвердились по коду и исправлены.

Исправления по существу:

- **Обрезка имени в архиве — по байтам, не по символам** (`_truncate_bytes`):
  для CJK 120 символов = 360 байт и снова `Errno 36`. Проверено в контейнере:
  имена из 64, 124 и 204 символов дают запись ~201 байт — в пределах лимита ФС.
  Обе функции (`_truncate_bytes`, `_safe_arcname`) вынесены на уровень модуля:
  вложенные нельзя ни импортировать, ни протестировать.
- **`zf.writestr` в потоке**: сжатие `documents_meta.json`, `chat_history.json`,
  алиасов и настроек шло в основном потоке — теперь через `asyncio.to_thread`.
- **`/backup-restore`**: один поток на namespace (`_restore_ns`) вместо
  `to_thread` на каждый ключ (было 1000 переключений на 1000 ключей).
- **`/deploy`**: `encoding` по умолчанию `utf8` (base64-подобный литерал больше не
  декодируется молча); `auto` декодирует только строки, похожие на base64; пустой
  файл записывается (проверка на `None`, а не на falsy). Проверено живьём:
  литерал `aGVsbG8=` с настройкой по умолчанию сохраняется как текст (8 байт),
  с явным `base64` — как `hello` (5 байт), пустая строка — 0 байт.
- **`/backup`**: `_all_config_categories` возвращает `None` при ошибке БД и `[]`
  при пустой таблице — 503 теперь только при реальной ошибке, а не на свежей
  установке. В `/backup-documents` при недоступной БД пишется маркер ошибки,
  а не пустой `config_store.json`.
- **`restart-ollama`** при исключении отдаёт JSON `{status: error, message}` —
  страница админки показывает текст, а не «❌ undefined» (проверено: HTTP 200,
  `status=warning` при недоступном хосте).
- Мелочи: `import traceback` в шапке; дубли `from pydantic import BaseModel` и
  `Optional as Opt` убраны; инициализация настроек при старте логирует сбой
  вместо молчаливого `pass`; `_graph_model_config` объявлена до загрузчика;
  worker считается по compose-метке, а не по подстроке в имени.

## Косметика по пятому разбору (2026-09-13, образ 2026.09.13.11)

Шесть замечаний «на будущее» закрыты, раз уж файл в работе:

- `_restore_namespace` вынесена на уровень модуля (создавалась заново в цикле по
  namespace);
- убран неиспользуемый `from sqlalchemy import text as _text` в блоке настроек;
- `get_session_local` поднят в начало `backup_documents`: импорт жил внутри
  try-блока настроек, а используется ещё и в блоке истории чатов — при падении
  первого второй записал бы `{"error": ...}` вместо истории. Проверено: в архиве
  `chat_history.json` есть и без ошибки;
- `import json, traceback` → `import json` (traceback уже в шапке модуля);
- `import subprocess` перенесён в шапку, локальные копии убраны;
- **base64 с переносами строк теперь принимается**: пробелы убираются до строгой
  проверки. Копирование base64 из терминала — обычный случай, а строгость
  сохраняется: мусор по-прежнему отвергается. Проверено живьём: многострочный
  base64 записался и дал текст `hello-multiline`, мусор — «Некорректный base64».

Контрольный круг после правок: `/backup-documents` → 200, 25.0 МБ, 51 запись;
`/backup-restore` → `restored: 9, errors: []`, категории до и после совпадают.

## Разбор роутов /kg (2026-09-13, образ 2026.09.13.14)

Проверено сканом роутов: из 20 эндпоинтов `/api/v1/kg` **4 были вообще без
зависимости прав** (GET `/domain-schema`, GET `/watchdog/status`,
GET `/type-watchdog/status`, POST `/type-watchdog/start`), остальные уже имели
admin или «любой авторизованный». Утверждения «/cypher и /rebuild-graph без
Depends» не подтвердились — они были admin.

Исправлено:

- POST `/type-watchdog/start` → `Depends(get_current_admin)` (была мутация без
  проверки); три GET получили явный `get_current_user_optional`. Роутов без
  проверки прав теперь 0 (проверено живьём: аноним → 401, не-админ → 403 на
  мутациях, чтение разрешено);
- **все 16 вызовов `kg_service.*`** (синхронный Neo4j) обёрнуты в
  `asyncio.to_thread`, включая `post_process_entities` и
  `deduplicate_entities_by_name`;
- `/rebuild-graph`: гонка check-then-act закрыта — добавлен атомарный
  `config_store.compare_and_set` (проверено: первый захват True, повторный с тем
  же ожиданием False, освобождение True);
- `execute_cypher`: вместо чёрного списка подстрок — требование начинать запрос с
  MATCH/RETURN/WITH/UNWIND + запрет CREATE/MERGE/SET/DELETE/REMOVE/DROP/CALL/
  LOAD CSV/FOREACH/APOC./DBMS. и лимит ≤1000. Проверено: чтение работает,
  CREATE/LOAD CSV/CALL → 400 («должен начинаться с…»), DELETE/SET → 400,
  limit=100000 → 1000 строк;
- `/stats`: 503 с текстом ошибки вместо нулей (нули выглядели как «граф пуст»);
- лимиты клампятся (`_clamp`), `scope` → `Literal` (мусор даёт 422),
  `type` → `entity_type` (обновлён kg.html), `q.lower()` считается один раз;
- пресет доменной схемы пишется через публичный `set_domain_schema`, а не в
  `_domain_config`;
- `type-watchdog/status`: `get_all` в потоке, один проход по документам;
- `hybrid-search`: `initialize()` вызывается только если клиента ещё нет (был
  лишний сетевой круг на каждый запрос);
- `/rebuild-status`: `transport_error` отличает сбой чтения статуса от падения
  задачи;
- импорт `kg_service` перенесён в шапку (цикла импортов нет — проверено).

Две ошибки в моих же правках, пойманные проверкой на стенде:

1. в модуле роутера не был импортирован `asyncio` — ВСЕ новые `to_thread` падали
   с `name 'asyncio' is not defined`, а эндпоинты отдавали ошибку в теле с кодом
   200. Добавлен импорт;
2. `compare_and_set` всегда возвращал False: `config_store.set` хранит строки без
   JSON-кавычек («idle»), а мой разбор вызывал `json.loads`. Добавлен
   `_decode_value` (JSON, иначе строка) — выровнено с `set()`.

Не подтвердилось из разбора: «/cypher и /rebuild-graph без проверки прав»
(уже были admin), «ключи payload как int» (`get_points_payload` нормализует к
str, поиск по str корректен).

### Ещё два дефекта /kg, найденные живьём (2026-09-13, образ 2026.09.13.15)

1. **`/cypher` отдавал HTTP 200 при любой ошибке** — с полем `error` в теле. Хуже:
   собственная `HTTPException(400, "Пустой запрос")` поднималась ВНУТРИ `try` и
   глушилась широким `except Exception`, поэтому пустой запрос тоже давал 200.
   Теперь: `except HTTPException: raise` идёт первым, синтаксическая ошибка Neo4j
   (`Neo4jClientError`) → 400, сбой соединения → 502. Успешный произвольный запрос
   пишется в лог с именем пользователя (`[cypher] admin: MATCH …`, обрезка 200
   символов). Проверено: синтаксис/пустой/APOC → 400, валидный → 200, строка в логе есть.

2. **Имена сущностей со слэшем давали 404.** В графе есть «ГОСТ Р ИСО/МЭК 15408-2-2013»,
   «Система управления изменениями/задачами», «ИН-03-23/95» — для них
   `/graph/{entity_name}` и `/entity/{entity_name}/chunks` не матчились (лишний
   сегмент пути). Пути переведены на `{entity_name:path}`. Проверено: фрагменты и
   граф отдаются (200, `chunk_seq` на месте), и снаружи через nginx тоже — на
   закодированный `%2F` и на литеральный слэш приходит 401 (значит путь доехал),
   а не 404.

Регрессия закрыта тестом `tests/test_kg_route_guards.py` (конвертер `:path` на обоих
роутах, порядок обработчиков в `/cypher`, отсутствие возврата ошибки с кодом 200,
аудит-строка). Итого в наборе: `test_kg_route_guards` + `test_admin_audit_fixes` +
`test_chunk_order` = 38 passed.

### config_store: строки читаются так же, как пишутся (2026-09-13, образ 2026.09.13.19)

Найдено на стенде при проверке пункта R5 обзора. `set()` сериализовал
dict/list/bool/int/float через `json.dumps`, а обычные строки писал СЫРЫМ текстом;
`get()` при этом ВСЕГДА вызывал `json.loads`. Что это давало:

- `set("running")` → `get()` → **None** (JSONDecodeError глотался внешним except);
- `set("123")` → `get()` → **int 123** (тип подменялся молча);
- поэтому `/rebuild-status` **никогда** не показывал «идёт перестроение»: статус
  это строка, в БД лежало сырое `idle`, которое не разбиралось, и читатель видел
  только запасное `or "idle"`. Сторож перестроения по той же причине не видел
  смены статуса.

Как сделано теперь: `set()` кодирует ВСЁ через `json.dumps` (строки — в кавычках),
`get()` разбирает через `_decode_value` (JSON, иначе — сырая строка, для записей
сделанных до правки). Проверено на стенде: round-trip точен для строк, чисел,
bool, словарей и списков; старые сырые записи (`active_preset = 'universal'`)
читаются строкой, а не None; `/rebuild-status` отдаёт `running`, если в настройках
running, и `idle` после сброса. Тесты — `tests/test_config_store_roundtrip.py`.

Плата за отсутствие типа в БД: сырое «123» без кавычек неотличимо от числа. Таких
записей в системе нет (из 3228 ключей сырых было 2, обе — слова), новые пишутся
в кавычках, поведение зафиксировано тестом.

Урок для похожих случаев: если значение пишут одним способом, а читают другим,
баг видно ТОЛЬКО на живом вызове (тот же класс, что `compare_and_set` и
`get_ext_llm()` без `await`). Проверка: `set(X)` → `get(X) == X` на стенде.

### Тела админских запросов переведены на Pydantic (2026-09-13, образ 2026.09.13.20)

Было: девять эндпоинтов `admin_models.py` и `update_domain_schema` принимали
`data: dict` — FastAPI ничего не валидировал, ошибки типа всплывали позже и
выглядели как 500, в OpenAPI схем не было.

Стало: `GraphModelConfig`, `ChatPromptConfig`, `BrandingConfig`,
`ProcessingBlockConfig`, `IngestBlockConfig`, `SearchModeConfig`,
`Neo4jConfigUpdate`, `DocTypeAction`, `WorkerResources` (admin_models.py) и
`DomainSchemaUpdate` (knowledge_graph.py). Проверено на стенде:

- валидные тела всех десяти эндпоинтов — 200, как раньше;
- неверный тип — **422** с внятной причиной (`bool_parsing`, `int_parsing`,
  `string_type`). Раньше, например, `{"blocked": "нет"}` молча превращалось в
  `False`, а `{"batch_size": "abc"}` давало 500 из `int()`;
- частичное обновление не затирает остальные поля;
- в OpenAPI 10/10 схем, `requestBody` ссылается на модель.

Детали, которые важно не потерять при следующих правках:

- обработчики различали «поле не прислали» и «прислали null» через `"key" in data`,
  поэтому работают с `payload.model_dump(exclude_unset=True)` — семантика та же;
- где тело сохраняется ЦЕЛИКОМ (`/graph`, ручная доменная схема) — `extra="allow"`,
  иначе неизвестные поля молча пропали бы при записи в config_store;
- `cpus`/`memory` приходят и строкой, и числом → `Union[str, float, int]`
  (Pydantic v2 не превращает float в str).

Попутно обнаружено: `/openapi.json` закрыт middleware (302 → `/login`), а страница
`/api/docs` публична и грузит спецификацию именно оттуда — то есть Swagger UI
не работает ни анонимно, ни с Bearer-токеном в браузере. Лечится либо открытием
`/openapi.json` (сейчас в нём нет секретов), либо переопределением
`openapi_url="/api/openapi.json"` с исключением в middleware.

Ещё одна находка того же класса, что и `/cypher`: в `knowledge_graph.py` осталось
около десятка `return {"status": "error", "message": ...}` с кодом 200 (watchdog,
validate, post-process, domain-schema и т. д.). UI части из них проверяет
`d.message`, поэтому перевод на 4xx/5xx нужно делать вместе с правкой страниц.

### Код ошибки не теряется: HTTPException пробрасывается во всех роутах (2026-09-13, образ 2026.09.13.23)

Класс бага, найденный на живом стенде дважды: `raise HTTPException(...)` стоит
ВНУТРИ `try`, а первым обработчиком идёт широкий `except Exception` — он её ловит
и подменяет ответ.

- `/cypher`: пустой запрос отдавал **HTTP 200** с полем `error` в теле;
- `/process`: новый 409 «уже в очереди» превращался в **500**.

Скан по исходникам синтаксическим деревом дал **38 таких цепочек в 6 файлах**:
`admin_models.py` 22, `admin.py` 5, `chat.py` 4, `web_monitor.py` 3,
`knowledge_graph.py` 2, `watchers.py` 2. Во всех добавлен
`except HTTPException: raise` ПЕРВЫМ обработчиком.

Проверено на стенде (аварийные пути и регрессия):

| запрос | было | стало |
|---|---|---|
| `/upload/{id}/process` при занятом замке | 500 | **409** «Документ уже в очереди…» |
| `/kg/cypher` пустой | 200 + error | **400** «Пустой запрос» |
| `/kg/cypher` синтаксис | 200 + error | **400** с текстом Neo4j |
| `/admin/models/qdrant/collections/нет/info` | 500 | **404** |
| `/admin/models/providers/нет-такого/test` | 500 | **404** «Провайдер не найден» |

Обычные пути без изменений (200): `/kg/stats`, `/kg/domain-schema`,
`/kg/rebuild-status`, `/admin/models/system/cpu`, `/docker/stats`, `/ssh-config`,
`/qdrant/collections`, `/upload/list`, `/kg/entity/<имя со слэшем>/chunks`.
Сплошной смоук 138 эндпоинтов: до правки 7 не-2xx, после — тот же набор (плюс
разовый 502 у `/admin/keycloak/users`, пока токен Keycloak был холодным сразу
после пересоздания контейнера; повторный вызов — 200).

Регрессия закрыта тестом `tests/test_http_exception_not_swallowed.py`: он
сканирует AST всех файлов роутов и падает, если HTTPException может попасть в
широкий except. Тот же тест был полезен как инвентарь — именно он показал, что
проблема не только в `upload.py`.

### Очередь: честные ответы и никакой синхронной работы в запросе (образ 2026.09.13.22→.23)

- `POST /{id}/reindex` **был синхронным**: `await document_service.process_document(...)`
  прямо в обработчике — запрос висел 6+ минут, клиентский таймаут обрывал работу
  на середине (документ оставался в `processing`), а параллельная задача worker'а
  приводила к записи графа двумя процессами (дедлок). Теперь постановка в очередь:
  ответ **81 мс** вместо минут, статусы `queued` / `already_queued`.
- `POST /{id}/process` отвечает **409**, если задача уже стоит (раньше — «поставлено»,
  потому что результат `enqueue_document` игнорировался).
- `/process-all` и `/reindex-all` считают только реально поставленные и возвращают
  `skipped`.
- `documents.html` показывает 409 нейтральным тостом, а не красной ошибкой.

### Метрика технического долга: синхронный I/O в async (обновлять после каждой пачки)

Скан — измерительный прибор, а не разовая работа. Команда:

```bash
python reports/_scratch/scan_sync_io.py src
```

Он обходит все async-функции проекта и ищет блокирующие вызовы (ФС, БД, Qdrant,
Neo4j, HTTP, subprocess, docker SDK, celery `inspect`), не обёрнутые в
`asyncio.to_thread`. Показывает: сколько вызовов, сколько **уникальных мест**
(файл + функция + вызов без литералов — чтобы `config_store.get(...)` в трёх
ветках одной функции не считался за три), разбивку по файлам и по видам.

| дата | вызовов | уникальных мест | upload.py | что сделано |
|---|---|---|---|---|
| 2026-09-13 | 258 | — | 56 | первый скан (по вызовам, Path одним видом) |
| 2026-09-13 | 236 | 197 | 34 | правки upload.py: 29 обёрток `to_thread`, `find_file` вместо `iterdir`, лимит scroll с флагом `truncated` |
| 2026-09-13 | 233 | 194 | 32 | второй порядок: chat_service (список документов), chunks.py (scroll), upload (`_save_document_to_db`, `_find_by_hash`) |
| 2026-09-13 | 225 | 186 | 32 | процесс API: HotFolderWatcher (ФС в цикле опроса), страницы (`_html_response`), watchers (`db.query` в async) |

Читать карту так: `config_store` 100 и «Path: проверка (дёшево)» 39 — это не 139
проблем, а 139 вызовов; оборачивать имеет смысл то, где есть реальное ожидание
(Qdrant 9, Neo4j 5, urllib 7, subprocess 4, архивы 5, «Path: обход каталога» 3,
«Path: чтение/запись файла» 2). Остальное — по мере надобности, начиная с
горячих путей.

### /queue: `inspect` ждёт воркеров

`celery_app.control.inspect()` — блокирующий вызов: три команды (active,
reserved, scheduled) при таймауте 0.7 с дают ~2 с на холодный запрос. Он в
`to_thread`, поэтому loop не блокируется, а результат кэшируется на 5 секунд
(замер: 2.9 с → 0.03 с → 0.03 с, после истечения TTL снова ~2.8 с).

Важная деталь кэша: **метку времени надо ставить ПОСЛЕ работы**. Если поставить её
до (как было в первой версии), то при TTL меньше длительности самого запроса
запись просрочивается раньше, чем приходит следующий вызов, и кэш не работает
никогда. Поймано живым замером: три вызова подряд по 6 с.

### Второй порядок: блокирующий I/O за синхронным хелпером

Первый скан видит только ПРЯМЫЕ блокирующие вызовы в async-функциях. Но горячий
путь часто такой:

    async def generate_response(...):          # async-метод
        meta = self._build_documents_list_context(...)   # СИНХРОННЫЙ хелпер → БД

Такой вызов на блокирующий не похож. Второй сканер
(`reports/_scratch/scan_sync_helpers.py`) строит карту функций проекта, находит
вызовы СИНХРОННЫХ сервисных методов из async-кода и разворачивает их на глубину 2.
На первом прогоне: **170 мест**, из них с настоящим блокирующим I/O (не короткий
`config_store.get`) — **23**.

Важно при доработке сканера: сопоставлять вызовы нужно по получателю
(`config_store.get`, `self._helper`), а не по «хвосту» имени — иначе `app.get` и
`status.get` ложно указывают на `config_store.get` и карта превращается в 1121
ложное место.

Что закрыто по итогам второго порядка: `chat_service.generate_response` (список
документов из БД — на каждый запрос чата; в `chat_service` вообще не было
`import asyncio`), `upload` (`_save_document_to_db` ×2, `_find_by_hash`),
`chunks.py` (scroll в поток + `truncated`/`scroll_limit`). Проверено живьём: чат
отвечает (мета-запрос 4.2 с, RAG 7.3 с, 10 источников), `/chunks` 200 с флагом,
`/check-duplicate` 200.

### Что в процессе API, а что в воркере (важно для приоритетов)

Одинаковый по виду вызов имеет разную цену в зависимости от того, где исполняется:

* **процесс API** (uvicorn, один event loop): `HotFolderWatcher` стартует в
  `lifespan`, страницы отдаются обработчиками, роуты чата/документов — блокировка
  здесь тормозит ВСЕХ пользователей;
* **воркер Celery** (solo-пул): там синхронный код нормален — он и так один
  поток. Поэтому `web_monitor._check_rss` (блокирующий fetch уже в потоке через
  `run_in_executor`), `document_service._process_document_impl`,
  `web_monitor._download_and_upload` в API-цикле не участвуют.

Практический вывод при разборе карты: сначала смотреть, откуда вызывается функция
(роут API, lifespan или задача Celery), и только потом решать, нужен ли `to_thread`.

Отдельно про синхронные обработчики: если роут объявлен `def` (не `async def`),
FastAPI сам исполняет его в пуле потоков — `to_thread` внутри не нужен и даже
вреден (вложенный поток). В `routes/watchers.py` такие есть (`remove_watched_url`
и другие) — их правильно не трогать; async-роуты (`check_single_url`) — трогать.
