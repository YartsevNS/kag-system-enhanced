# Состояние работы — KAG (обновлено 2026-09-18: переход из /kg к странице фрагмента — ждёт выкатки)

Файл для возобновления после сжатия контекста или в новой сессии.

**Важно про расположение.** Раньше этот файл лежал в `.hermes/handoff.md` и больше не
обновляется: Hermes блокирует запись в файлы внутри project-local `.hermes/`
(жёсткое правило стража «protected instruction files», запись требует интерактивного
подтверждения, которое в CLI-сессии не приходит). Файл `.hermes/handoff.md` остался
**устаревшим** — не верить ему. Актуальное состояние: этот файл + `docs/guides/sync-io-audit.md`
+ память Mnemosyne (`project:kag/*`, `task:progress/*`).

## Инцидент 19.09 (ВАЖНО): падает хост pve1, а не стенд

Стенд 18 = ВМ **107** (`servertest`) на узле **pve1** (`192.168.50.26`, единственный узел,
кластера нет). 19.09 хост pve1 обрывался **девять раз** (13:57, 14:13, 15:02, 15:19, 15:53,
16:54, 16:56 — 25 секунд жизни, 17:09, ~17:2x). В журналах предыдущих загрузок нет ни
`Reached target Shutdown`, ни паники, ни OOM, ни `hung task`, ни ошибок I/O — журнал просто
обрывается: в PVE взведён `softdog` (60 с), поэтому зависание заканчивается аппаратным сбросом
и следа не оставляет. Последние падения шли при выключенном стенде, то есть моя нагрузка
перестала быть объяснением.

Аппаратные улики: NVMe **Samsung PM991a 256 ГБ** (`/mnt/pve/rev`) — SMART **FAILED**,
Critical Warning 0x04, износ **99 %**, 88.5 ТБ записи, 44 ошибки целостности, 34 мин выше
критической температуры; на нём диск ВМ 102 «Docker». Плюс один `mce: [Hardware Error]` в
журнале ядра (15:38); EDAC по памяти нулевой. SATA-диски чистые, диск стенда (ВМ 107,
хранилище `data`) здоров.

Полный разбор, доказательства, гипотезы и порядок завершения обновления —
**`docs/guides/host-hardware-incident.md`**.

Следствия для работы:

- обновление 18 ЗАВЕРШЕНО (19.09, вечер): Debian 12.15, ядро 6.1.0-53, перезагрузка сделана,
  все 11 контейнеров healthy, api 200, сайт 307 (редирект на логин), чат отвечает. Первая попытка
  обрывалась падением хоста — initrd нового ядра не собрался, ВМ ушла в kernel panic; вылечено
  через консоль Proxmox (загрузка старого ядра 6.1.0-32 в GRUB → `dpkg --configure -a` → перезагрузка);
- реестр документов ПРОПАЛ и ВОССТАНОВЛЕН: таблица `documents` была пуста при целых Qdrant
  (5025 точек), файлах (177) и графе (82 Document). Собрана заново из карточек Qdrant + дампа 29.08
  скриптом `reports/_scratch/restore/restore_documents.py` (запуск внутри `kag-api`): 81 строка,
  сумма chunks_count 4944, домены 53/24/4, список API → 81, details+preview 200 для всех 81,
  сирот 0. Дамп до правки — `backup_20260919/kag_before_restore.sql`;
- вероятная причина потери (не доказана, логи прокрутились): `_cleanup_stale_records()` в
  `document_service.py:141` удаляет на старте записи без файла на диске, а при недоступном
  `/app/data/uploads` уходит в fallback `/tmp` и удаляет ВСЕ документы. Нужен guard + WARNING;
- второй фактор: диск ВМ 107 подключён `cache=writeback` — при жёстком сбросе хоста подтверждённые
  записи могут не дойти до диска; стенду разумнее `cache=none`;
- ВМ 107 выставлен `onboot=1` — стенд поднимается сам после падения хоста;
- снимки ВМ 107: `pre-update-20260919` (16:42) и `after-update-20260919` (18:02);
- на стенде `yartsevn` получил sudo без пароля на apt/dpkg/docker/docker-compose/systemctl
  (`/etc/sudoers.d/hermes-agent`, откат — `rm` этого файла); это фактически полный root;
- Docker на 18 — пакеты Debian (docker.io 20.10.24, compose 1.29.2); переезд на docker-ce 28.x —
  отдельный шаг с правкой деплой-скриптов под `docker compose`;
- ВЫКАЧЕНО 19.09: образы **2026.09.19.3** (api + worker; собраны и запушены со стенда), пересозданы
  по одному api → worker → worker-maintenance, все healthy, api 200, 81 документ, чат отвечает;
- чат: reasoning-модель тратила весь лимит токенов на размышления и отдавала пустой content
  (при 200–700 токенах — 0 символов). Галочка «Отключить размышления модели (no think)» в привязке
  функции (Админка → Модели LLM) раньше действовала только на извлечение графа; теперь chat_service
  читает parameters.no_think и для deepseek/openai/openrouter-совместимых шлёт
  `thinking.type=disabled`. Проверено живьём: с max_tokens=200 ответ 653 символа (было 0).
  Замер параметров: `thinking:{type:disabled}` и `reasoning_effort:none` работают, `no_think:true`
  и `chat_template_kwargs` — нет;
- диск ВМ 107 переведён с `cache=writeback` на `cache=none` (durability при жёстких сбросах);
- настройки генерации чата: приоритет «запрос → привязка функции → встроенный дефолт (0.7/4096)».
  UI чата больше не шлёт хардкод `temperature 0.7 / max_tokens 2048` (из-за него настройки админки
  не действовали никогда), в форме привязки функции появилось поле max_tokens (раньше было жёстко
  4096 и не редактировалось) и подписи с обоснованием каждого значения. Тесты:
  tests/test_chat_generation_params.py (5);
- глубина контекста: ЗАМЕРЫ 19.09 показали, что ни уменьшение до 5 фрагментов (−0.15 балла,
  экономия 0.8 с), ни пометка лучшего фрагмента (−0.03) точность не улучшают → по умолчанию
  `context_limit = 10`, `mark_best = false`, `min_top_score = 0`. При этом `context_limit`
  доступен КЛИЕНТУ на каждый запрос (UI-селектор «Контекст» 3/5/8/10/15/20 + поле в API,
  границы 3..20 зажимает сервер, в лог пишется эффективная глубина и её источник).
  Рычаги реализованы в привязке функции chat, тесты: tests/test_chat_context_params.py (11);
- проверка консистентности реестра после загрузок/удалений: `scripts/check_registry_consistency.py`
  (запуск в контейнере api) — БД ↔ Qdrant ↔ файлы, сироты, расхождения chunks_count.
  19.09 по ней: 81 документ, 5025 точек, 81 карточка, расхождений нет; на диске 96 файлов
  от ранее удалённых документов (приложение удаляет записи и векторы, файлы остаются);
- резервный провайдер для чата: `FunctionMap.fallback_provider_id/fallback_model` (админка →
  привязка функции). Вызов идёт по цепочке «основной → резерв»: не ответил (пустой content,
  ошибка, таймаут, ответ «❌…») → пробуем резерв, в журнале «Ответ дан РЕЗЕРВНЫМ провайдером»,
  в ответе API metadata.provider / metadata.fallback_used (раньше метаданные роута теряли флаги —
  теперь сервисные поля пробрасываются). На стенде настроено: deepseec/deepseek-flash →
  polza.ai/z-ai/glm-5.3-flash. Проверено живьём: с «мёртвым» основным ответ пришёл от polza
  за 4.7 с (fallback_used=True), после восстановления — от deepseec (fallback_used=False).
  ВАЖНО: кэш провайдеров в provider_service не истекает — правка провайдера прямо в БД
  подхватится только после перезапуска api или сохранения через админский API.
  Образ api 2026.09.19.8, тесты tests/test_chat_fallback_provider.py (5). Резерв для обработки
  документов и графа ПОКА НЕ сделан (там свои прямые вызовы провайдера); выбор провайдера
  для конкретного документа (страница «Документы») — тоже не сделан, ждёт решения;
- ВЫБОР МОДЕЛИ В ЧАТЕ (20.09, образы api/worker 2026.09.19.11): название модели в шапке чата стало
  кликабельным — открывается список «Как в настройках (… · резерв: …)» и все включённые
  провайдеры с моделями (GET /chat/providers, без ключей). Выбор запоминается в браузере и
  уходит в каждый запрос (provider_id/model); приоритет «запрос → привязка функции», неизвестный
  или выключенный провайдер игнорируется. Резервом при выборе остаётся резерв из привязки.
  Тесты: tests/test_chat_provider_choice.py (5);
- РЕЗЕРВ ДЛЯ ОБРАБОТКИ ДОКУМЕНТОВ: `provider_service.get_function_llm_chain(function)` даёт
  [основной, резервный] конфиги; document_analyzer идёт по плану «основной ×2 → резерв ×2»
  (в журнале «пробую РЕЗЕРВ provider/model»). На стенде doc_analysis: deepseek-v4-flash →
  polza.ai/z-ai/glm-5.3-flash, проверено: цепочка из 2 конфигов, обычный анализ работает
  (детали и тесты — tests/test_llm_chain_for_pipeline.py, 5). Извлечение графа (entity_extractor)
  переведено на ту же логику типов, но собственный цикл повтора с резервом там ещё НЕ сделан;
- ИСПРАВЛЕН ДЕФЕКТ: тип провайдера «custom» (так добавлен polza.ai) в анализе документов,
  извлечении графа и перестройке графа считался Ollama — запрос шёл на /api/generate вместо
  /v1/chat/completions и шаг падал. Теперь «custom» и «gigachat» идут по OpenAI-совместимой
  ветке, для custom шлём thinking.type=disabled. До этой правки связать polza/GLM на анализ
  документов было нельзя;
- кэш провайдеров и привязок получил TTL 30 с (PROVIDER_CACHE_TTL): раньше правка в админке
  инвалидировала кэш только процесса, через который сохраняли, и воркеры работали по старой
  привязке до перезапуска — выглядело как «поменял, а не применилось»;
- на ключе polza доступны z-ai/glm-5.3-flash и deepseek/deepseek-v4.1-flash; qwen (qwen3-max,
  qwen3-235b), glm-4.7-flash, glm-5.3, gpt-5-mini отдают 403 «модель недоступна для ключа»;
  локальный GigaChat через gpt2giga-прокси — отдельный провайдер (годится вторым резервом);
- выбор провайдера для конкретного документа на странице «Документы» — НЕ делаем (решение
  пользователя 19.09): обработка идёт по привязке функции;
- РЕЗЕРВ В СЛУЖЕБНЫХ ВЫЗОВАХ ЧАТА И В ГРАФЕ (образы 2026.09.19.12): классификатор домена
  (query_analysis) и разбиение вопроса идут по цепочке «основной → резерв» с таймаутом 30 с
  вместо 120 (именно они дали ответ за 122 с при генерации 2.3 с); в извлечении графа
  entity_extractor._call_llm — обёртка: пустой результат основного → повтор на резерве функции
  graph (в журнале «пробую РЕЗЕРВ provider/model»). Тесты: tests/test_chat_internal_calls_fallback.py (4),
  tests/test_graph_reserve_fallback.py (2). На стенде резерв для graph выставляется в админке
  так же, как для chat/doc_analysis;
- железо 19.09 (вечер): пользователь перевёл ВМ 102 на `bigbuck` (HDD) и очистил NVMe — хранилище
  `rev` пустое (2.1 МБ из 234 ГБ), данных на больном диске больше нет; SMART у него по-прежнему
  FAILED (99 %, 45 ошибок целостности) — вынуть при удобном случае.
- тяжёлые замеры по 18 не гонять, пока железо не вылечено (для тяжёлого есть 41 или локальная машина).

## Где мы сейчас

- Репозиторий (ноутбук): `C:\VSCODE_PROJECT\kag-system-enhanced`, ветка **PREPROD**,
  последний коммит на момент правки этого файла — **3d509a3** (этот файл + гайд по качеству
  ответов правились заходом 17.09), дерево чистое.
- Сервер 18 (`yartsevn@192.168.50.18`, `/home/yartsevn/kag-system`): `kag-api`,
  `kag-system_worker_1`, `worker-maintenance` на образе **2026.09.19.1**.
- **Сделано (19.09), ВЫКАЧЕНО: страница «Опыты и модели» и раздел «Сироты» в админке.**
  - Страница `/experiments` (только админ, пункт меню появляется по `/auth/me`): сравнение
    эмбеддеров на нашем корпусе, блок «Железо под GPU», история замеров, методика. Данные
    лежат в `config_store` → `experiments/models`, читаются через
    `GET /api/v1/admin/models/experiments` — обновляются без пересборки образа.
  - По карточкам моделей: «~115 тыс. ток/с» относится к `10B-A1.8B` (114.5k), у `3B-0826`
    87.9–91.5k; замер на H100, batch size 16. Для корпуса 4944 чанка достаточно одной карты
    24 ГБ (L4 49.74 ₽/ч, 4090 66.63 ₽/ч; покупка 4090 от 140 тыс. ₽). 3B на CPU (20 ядер)
    даёт ~10–20 с на фрагмент → полная переиндексация около суток, то есть без GPU непрактично.
  - Раздел «Сироты в векторном хранилище» в админке: суточный скан (Celery beat,
    `scan_orphans`, очередь maintenance) + кнопки «Проверить сейчас», «Удалить точки»,
    «Игнорировать». Автоматика сирот НЕ удаляет: штатные паузы обработки (блокировка,
    пополнение счёта, перезапуск после питания) должны переживать проверку, решение за админом.
- **Разбор сирот (19.09): откуда они берутся — доказано экспериментом.** Служебный документ
  (588 чанков): удаление при 24 точках → задача дописала ещё 524 точки и завершилась успешно
  (в журнале её же предупреждение «точек в Qdrant 548, а чанков 588»). Причина: `tasks.py`
  проверяет существование документа только в начале, а пишет векторы в конце — из объекта
  в памяти; удаление работает фильтром по `document_id`, поэтому точки, записанные после
  удаления, никто не убирает. Второе следствие: `repo.upsert` = «найти или создать»,
  поэтому идущая задача ВОССТАНАВЛИВАЕТ удалённый документ (в прогоне строка возвращалась,
  и документ закрылся статусом «обработан» с неполными векторами). Историческая партия
  сирот (1428 точек / 89 id на 12.09) — от поведения до 28.08, когда удаление вообще не
  чистило Qdrant (см. коммит `0595842`). Закрыть корень = не писать векторы документу,
  которого уже нет, и не воскрешать удалённый (обсуждается, решено пока показывать в админке).
- **Сирот на стенде сейчас нет**: 5025 точек, 81 document_id, 81 документ — множества совпадают.
- **Сделано: триаж чанков** (`src/indexing/chunk_triage.py`) — штампы (копия чанка ещё
  минимум в 2 ЧУЖИХ документах, косинус ≥ 0.95) и внутридокументные дубли (≥ 0.95) не
  отправляются в LLM-извлечение; узлы чанков создаются, штампы помечаются
  `Chunk.is_boilerplate = true`, шаг `graph_triage` виден в журнале обработки.
  Настройки: `GRAPH_TRIAGE_ENABLED`, `CHUNK_BOILERPLATE_SIM` (0.95),
  `CHUNK_BOILERPLATE_MIN_DOCS` (3), `CHUNK_DUP_SIM` (0.95). Детали, замеры и три ловушки —
  `docs/guides/chunk-triage.md`.
- **Сделано (18.09), ВЫКАЧЕНО НА СТЕНД: навигация из /kg к фрагменту и странице.**
  Причина жалобы «нажимаю на страницу — ничего не открывается»: у узлов графа не было
  не только страницы, но и id точки Qdrant (в Neo4j страницы нет), а переход шёл через
  `window.open` (глушится блокировкой всплывающих окон). Сделано: узлы-фрагменты несут
  `qdrant_point_id` (`search_chunks`, `get_document_graph`), роут графа обогащает узлы
  полем `page` одним запросом payload'ов (`_attach_pages`, ошибка Qdrant граф не ломает);
  в /kg переход — настоящая ссылка `target=_blank` с `&page=` и `&q=`, узел-документ
  открывается на странице первого найденного фрагмента, в панели сущности совпадения
  идут первыми (затем — нарисованные в графе), термины подсвечены; просмотрщик по `q`
  подсвечивает термины в текстовом слое страницы.
  **Проверено живьём 18.09:** Е2Е в worker-контейнере — фрагмент 42 → `/viewer&page=27`
  → открылась страница 27 из 29, подсвечено «ЦФА, цифровой рубль, крипторынок»;
  в поиске по тексту 43/43 фрагмента со страницей, в графе сущности 15/15; JS-ошибок нет.
  Тест `tests/test_kg_fragment_navigation.py` (6), локальный прогон набора 515 passed.
  Детали — `docs/guides/graph.md` (раздел «Переход от фрагмента к странице документа»),
  Е2Е — `scripts/e2e_kg_fragment_nav.py`.
  **ВНИМАНИЕ (состояние выкатки):** ssh до стенда открыт на внешнем адресе
  `77.37.242.130:2223` (порт 22 закрыт, доступ по trust-list). Правка накатана
  стопгапом — файлы скопированы в контейнеры `docker cp` — потому что образ собрать
  было нечем (локальный Docker Desktop не запущен, `kre44et/kag-base:2026.09.07` на
  сервере отсутствовал). База стянута на сервер, образы `2026.09.18.1` собираются там
  же; после `docker-compose up -d --force-recreate api worker worker-maintenance`
  стопгап перестаёт быть нужным. Если образы НЕ собраны и контейнеры пересозданы —
  правка пропадёт, файлы надо положить заново.
- **Сделано: карточка документа** (`src/indexing/document_card.py`) — контекстуальный префикс
  эмбеддинга собирается из УЖЕ существующего анализа документа (title/type/summary/topics),
  дополнительных LLM-вызовов нет. Анализ перенесён ДО векторизации; префикс ≤200 символов идёт
  в metadata чанков (`build_embedding_text` → «<карточка> · п. 5.2.1: <текст>»); карточка пишется
  отдельной точкой `level=document` (поиск фрагментов её исключает, есть `search_documents`).
  Заодно починены три скрытых дефекта: поля анализа затирались финальным сохранением конвейера
  (было 0 из 51 документа с названием), анализатор делал одну попытку и падал в {} на не-JSON
  (разбор регекспом рвался на вложенных скобках/markdown), `qdrant.set_payload` не существует
  (payload с типом/summary не обновлялся никогда). Детали — `docs/guides/document-card.md`.
  A/B по длине префикса (три полных переиндексации корпуса, набор из 7 вопросов):
  hit@1 0.7143 (без карточки) / 0.5714 (200 симв.) / 0.7143 (80 симв. — по умолчанию);
  MRR 0.8214 / 0.7619 / 0.8214; precision@10 0.6286 / 0.8143 / 0.7857. Длинный префикс даёт
  покрытие и теряет первую позицию (одинаковый префикс у всех чанков перевешивает сам чанк),
  поэтому по умолчанию 80 (название + тип, без тем). Настройка CARD_PREFIX_ENABLED и
  CARD_PREFIX_MAX_CHARS прокинута в окружение api/worker.
  Оговорка: 7 вопросов, один = 0.14 — для различения «80 против выключено» набор расширить
  до 20-30 вопросов (нужно и для сравнений из пункта 4).
- **Сделано (вечер 2026-09-13): разделы + реранкер + оценка ответов.**
  * Слой разделов: 248 узлов Section, 4353 чанка с крошкой; метрики на исторических 7 вопросах
    hit@3 0.857 → 1.0, MRR 0.821 → 0.833.
  * Реранкер: нашлись три дефекта (модели BAAI/bge-reranker-v2-m3 у flashrank нет → 404;
    вместо «выключено» молча включался BM25 и падал на делении на ноль; numpy-оценка ломала
    сериализацию ответа API). Теперь это настройка в админке (вкл/выкл + 5 моделей, по умолчанию
    мультиязычная MultiBERT), кэш в /app/data/models/flashrank, статус и кнопка предзагрузки.
    Замер: с включённым 0.50 против 0.55-0.61 без него → ВЫКЛЮЧЕН по замеру.
  * Служебные фрагменты (титул/оглавление/колонтитул) больше не вытесняют содержание из
    контекста чата — уходят в конец (src/indexing/service_chunks.py).
  * Набор вопросов v3: 35 (7 исторических + 20 с эталонными ответами владельца + 4 сравнительных
    + 4 контрольных); измеритель качества ОТВЕТА (LLM-судья по рубрике) + проверка живости эталонов.
      История правок — docs/guides/reranker-and-answer-quality.md.
  * Сравнительный режим (пункт 4): реализован (разбиение сторон без LLM, контекст по сторонам
    на карточках документов + фрагменты с крошкой раздела, инструкция «Общее/Отличия»),
    НО выключен по замеру: покрытие сравниваемых документов 0.75 против 0.875 без режима —
    маппинг «сторона → документ» через карточки ненадёжен на серии документов (обе стороны
    сводились к одному документу; ГОСТ 34.11/34.13 подменялись на 34.10/34.12).
    Гипотеза исправления и инструменты — docs/guides/comparison-mode.md.
  * Битый текстовый слой PDF (ночь 2026-09-14): у 5 документов из 61 текст был записан неверной
    кодировкой («ÔÅÄÅÐÀËÜÍÎÅ» вместо «ФЕДЕРАЛЬНОЕ»), поэтому не находился по русским запросам.
    Починено в hybrid_parser: восстановление кодировки (посимвольно latin-1 -> cp1251), а при
    невозможности — OCR; попутно: OCR больше не прогоняется заново на каждой странице и результат
    разбора сохраняется с document_id (в корпусе пять файлов с именем r-1323565.1.pdf затирали
    друг друга). Замер: средний балл ответов 0.50 -> 0.635, вопрос про S3G 0.00 -> 1.00.
    Детали — docs/guides/pdf-broken-text-layer.md.

  ПОБОЧНЫЙ РЕЗУЛЬТАТ: анализ документа починен — 52 из 52 документов имеют название, тип и
  темы (было 0 из 51). Это влияет на типизацию, payload чанков и интерфейс, не только на карточку.
- Загрузка новых документов на стенде заблокирована галочкой (`UPLOADS_BLOCKED`);
  документов 47, все `completed`.
- Тесты: `test_warm_init_invalidation` (5), `test_config_store_cache` (5),
  `test_type_watchdog_registration` (15), `test_config_store_roundtrip`,
  `test_processing_block_guard` (10), `test_kg_route_guards`, `test_document_access`,
  `test_upload_route_guards`, `test_api_process_io`, `test_domain_schema_persistence` (17),
  `test_admin_audit_fixes`, `test_no_secrets_in_repo`, `test_chunk_order`.

## Главное этой сессии: карта синхронного I/O в async

Карта: **258 → 166 мест (145 уникальных)**. Инструмент — `scripts/scan_sync_io.py`
(в репозитории; сопоставление по получателю, иначе `app.get`/`status.get` дают тысячи
ложных). Обоснование остатка — `docs/guides/sync-io-audit.md`.

Закрыто (всё подтверждено замером на стенде):

| Файл | Что | Замер до → после |
|---|---|---|
| `embeddings_service.py` | 23 вызова синхронного Qdrant-клиента в 12 async-методах, включая `search()` — путь каждого запроса чата | чат 5 с в фоне → `/health` 1.4–2.4 мс |
| `embeddings_service.py` | `initialize()` звал пробный эмбеддинг на каждый запрос | 163 мс каждый раз → 971 мс холодная / 0.0 мс тёплая |
| `embeddings_service.py` | окно переинициализации 60 с при смене модели | подпись модели: смена → переинициализация сразу |
| `type_watchdog.py` | ФС/БД сторожа в его цикле → в поток | `/health` 2.2–3.1 мс во время работы сторожа |
| `routes/admin.py` | Keycloak (`urllib`) и `subprocess.check_output` в async → в поток | `/admin/keycloak/users` 0.387 с, `/health` 2.2–3.1 мс |
| `config_store.py` | `get` = 10.4 мс (не короткий SELECT) → кэш чтения 2 с внутри хранилища; затем отсутствующий ключ читался из БД всегда → отрицательный кэш | 10.4 → 0.001–0.007 мс (85 мест разом); отсутствующий ключ 1.1 → 0.001–0.002 мс |
| `routes/upload.py` | 4 × `get_all()` в async-роутах → в поток; остальные 19 мест обоснованы построчно | `POST /reprocess-pending` 0.049 с |
| `routes/chunks.py`, `chat_service.py`, `main.py`, `hot_folder_watcher.py`, `routes/watchers.py`, `routes/knowledge_graph.py`, `indexing/knowledge_graph.py` | горячие пути в поток + флаги (`truncated`, `scroll_limit`) | `/chunks` total 3495; чат-мета 4.2 с / 47 документов |

SLA по замерам: **`/health` под нагрузкой чата ≤ 10 мс**.

Уроки (записаны в скилл `kag-admin-debug-deploy`):

- одинаковый вызов стоит по-разному в процессе API и в воркере Celery (solo-пул);
- AST-правка массово: оборачивать весь `ast.Call` целиком (цепочка `.attr` ломается);
  после массовой правки сервиса — дымовой тест по всем затронутым методам;
- `HTTPException` внутри `try` не должна попадать в широкий `except`;
- решать по замеру: `config_store.get` 10.4 мс, `repo.get` 1.3 мс, `repo.upsert` 5.0 мс;
- при правках файлов скриптом нормализовать окончания строк (якоря CRLF/LF).

## Тесты: набор полностью зелёный (2026-09-13)

**Полный прогон: 380 passed, 0 failed** (было 4 failed, 376 passed). Разбор:

| Тест | Причина | Правка |
|---|---|---|
| `test_auth.py::test_me_wrong_secret` | `from jose import jwt`, а python-jose в зависимостях нет | переведено на PyJWT |
| `test_kg_admin.py::test_admin_ops_forbidden_for_regular_user` | в `ADMIN_OPS` пути `/kg/watchdog/start|stop`, которых нет (есть `GET /watchdog/status`, `POST /type-watchdog/start`) → 404 вместо 403 | список сверен с роутером; стража проверена на реальных роутах |
| `test_security.py::test_generate_key` | два `GOSTCrypto()` с путём по умолчанию читали ОДИН файл ключа → «ключи разные» падало | `tmp_path` на экземпляр + проверка, что тот же файл даёт тот же ключ |
| `test_security.py::test_rate_limit_exceeded` | 150 запросов, растянутых на 150 с, при окне 60 с → в окне 60 < 100, лимит не превышен | 150 запросов в 30 с > 100 |

Плюс ранее в этот день: `test_api.py` 7 падений → 16 passed, `test_monitoring.py` 18 → 32
passed (причина: auth-middleware + SetupCheck + пустая тестовая SQLite из-за StaticPool и
неимпортированных `monitoring_models`).

**Где что лежит:** токен для тестов — фикстура `auth_headers` в `tests/conftest.py` (секрет
— уникальный фейк на прогон через `monkeypatch`). Подмена `_is_configured` — в фикстуре
`client` файла `test_monitoring.py` (снимается автоматически).

**Урок:** красный набор скрывал два реальных бага (`folder_watcher.remove_folder` → 500,
`doc-1`/`doc-10_other.pdf`). Ни один из 22 починенных тестов не удалён и не пропущен.

## Сессия 2026-09-13 (продолжение): тесты, folder_watcher, скилл

- **`src/monitoring/folder_watcher.py` — реальная ошибка, найденная тестом:**
  `Observer.unschedule()` требует `ObservedWatch` (объект от `schedule()`), а код передавал
  `FolderWatcherHandler` → `KeyError` из watchdog → **500** при снятии папки с наблюдения
  (`DELETE /api/v1/watchers/folders/{id}`). Исправлено: `_watches` хранит watch по папке;
  `start()` больше не хардкодит `recursive=True`. Проверено живьём: добавление и снятие
  папки → 200, список пуст.
- **Тесты починены, набор зелёный:** `tests/test_api.py` 7 падений → 16 passed,
  `tests/test_monitoring.py` 18 → 32 passed. Причины были не «в коде»: тесты писались до
  auth-middleware (ждали 200 без токена), патчили несуществующие атрибуты, а тестовая
  SQLite была пустой (in-memory живёт НА СОЕДИНЕНИЕ — нужен `StaticPool`, плюс импорт
  `monitoring_models`, иначе таблиц нет в схеме) и SetupCheck уводил запросы на `/setup`.
- **Скилл `kag-admin-debug-deploy` разбит**: SKILL.md 98 890 → 27 496 символов (упёрся в
  лимит 100k, правки не проходили). История вынесена в `references/`:
  `setup-wizard-and-secrets.md`, `deploy-recipes.md`, `history-incidents-2026-06-07.md`,
  `hybrid-search.md` (дословно), `nginx-upstream-resolver.md`. Бэкап:
  `SKILL.md.before-split-2026-09-13`. Указатель переписан, в архиве ~191 файл.

## Граф знаний: галочка «не строить» + найденный неполный граф (2026-09-13)

**Замер, из-за которого всё это:** документ «перечень документов по КИИ 2.pdf» (13 чанков)
обрабатывался 147 с, из них **135.7 с — граф**; разбор PDF + векторы — 11.6 с. У документа
на 212 чанков без графа: 21.8 с (parse 9.1, vectorize 10.3, analyze 2.3) — с `graph_skipped`.
Стоимость графа: ~3 с на чанк (2 LLM-вызова на чанк: сущности, затем связи) + дедупликация
сущностей, которая эмбеддит ВЕСЬ граф заново (~1577 имён при 8506 узлах = 197 батчей по 225 мс
≈ 44 с) + пары алиасов по одному запросу на пару.

**Сделано:** `system/graph = {skip}` + `processing_guard.graph_skip_requested()` (fail-open),
конвейер пропускает граф и entity resolution (шаг `graph_skipped` в журнале), API
`GET/POST /api/v1/admin/models/graph-build-config`, в админке секция «🧠 Граф знаний»
(галочка + «Построить граф сейчас»), на «Документах» — кнопка «🧠 Граф» у completed-документа.

**Найдено и исправлено попутно (важно!):** в `rebuild_graph_task` остался старый лимит
`chunks[:10]` — путь «построить позже» давал НЕПОЛНЫЙ граф: у документа в 23 чанка было
10 узлов-чанков (43%), у 212-чанкового было бы 4.7%. В конвейере лимит убрали ранее, здесь
нет. Исправлено (все чанки + отзывчивая остановка + отчёт `chunks_processed`/`chunks_total`),
проверено живьём: 10 → **23** узла, 51 сущность. Образ **2026.09.13.46**.
СЛЕДСТВИЕ ДЛЯ СТЕНДА: графы, построенные кнопкой «Перестроить граф» до этого образа, могут быть
неполными — по документам, где граф важен, стоит перестроить заново.

**СДЕЛАНО (коммиты 730f6ee + e4e48c2, образ 2026.09.13.48):** дедупликация по эмбеддингам
убрана из конвейера документа; кандидаты формирует ночная задача `resolve_entity_candidates`
в maintenance-очереди (auto_merge=False — слияния только по ревью). КОНТРОЛЬНЫЙ ЗАМЕР: полный
проход по графу = **131 с** и даёт **9 кандидатов** (при 8514 сущностях) — теперь это раз в сутки,
а не на каждый документ. Шаг графа у тестового документа: было 115–144 с → стало **4.8 с**.

**Следующие шаги (обсуждены, ждут решения):** кэш эмбеддингов имён сущностей (убирает ~44 с
с каждого документа), граф фоновой задачей, параллельное извлечение (пул 3–8), алиасы батчем.

## Инцидент 2026-09-13 (сайт лежал): nginx держал старый IP api

`--force-recreate api` меняет IP контейнера, а статический upstream nginx резолвится один
раз при старте -> **502 на весь сайт** до ручного `nginx -s reload`. Починено навсегда:
в `docker/nginx/conf.d/kag.conf` адреса upstream в переменных +
`resolver 127.0.0.11 valid=10s ipv6=off` — nginx перерезолвит сам (проверено: сайт
восстановился за <=3 с без вмешательства). Разбор — `docs/guides/deploy.md`.

Уроки: (1) проверять деплой ЧЕРЕЗ nginx, а не только `localhost:8000` — мои проверки шли
мимо nginx и 502 не видели; (2) `docker network connect` без `--alias api` ломает
DNS-запись -> 502; (3) `nginx -t` перед reload.

## Открыто (по убыванию полезности)

0. **НАЙДЕНО 18.09: жёсткий фильтр по домену в чате скрывает нужные документы.**
   `chat_service` передаёт домен из `query_analysis` в `embeddings_service.search(domain=...)`
   как ЖЁСТКИЙ фильтр. У документов, где детекция домена при индексации не сработала,
   `domain` пустой → они из выдачи выпадают. Замер: вопрос про 2-МР — с
   фильтром `domain=infosec` отказ «не найдена» и балл 0.00 (фактов 1/10), без фильтра —
   0.90 (фактов 7/10), ответ со ссылкой на п. 2.2. В коллекции 4944 чанка: `infosec` 2994,
   **без домена 1012 (20%)**, `accounting` 938; на 20 вопросах набора фильтр меняет top-1
   документ у 7 вопросов. Из 4 отказов этим объясняются два: 2-МР и «профиль защиты ПО»
   (без фильтра первым идёт нужный ИН-017-56/118); «стресс-сценарии ВПОДК» и
   «макропруденциальные надбавки» — выдача одинаковая, причина другая.
   План (ждёт решения): домен сделать мягким сигналом, а не отсечением — настройка
   `chat.domain` (`off`/`safe`/`hard`) в config_store, `safe` = не выбрасывать документы
   с пустым доменом + повтор без фильтра при бедной выдаче; A/B на 20 вопросах с базой
   в том же заходе; отдельно дозаполнить домены у 1012 чанков. Инструменты разбора
   (только чтение): `reports/_scratch/diag_2mr_domain.py`, `diag_2mr_spy.py`,
   `diag_domain_refusals.py`, `diag_domain_facets.py`.

1. **Построчные обоснования по остатку карты** — в момент правки файла, не отдельным
   проходом. Сделано: `upload.py` 19, `admin_models.py` 43 (правок не потребовалось —
   все находки либо в потоке, либо дешёвые по замеру). Осталось: `document_service.py` 16,
   `main.py` 14, `routes/knowledge_graph.py` 11. Формат: строка | вызов | замер | решение | почему.
2. Searchable PDF для сканов: наложить OCR-текст (`data/ocr_results/*.pdf.md`) на
   страницы исходника — у сканов нет текстового слоя.
3. Чистка git-истории от утёкших секретов (Neo4j — 16 коммитов, sudo — 2, JWT — 2);
   ротация `ADMIN_PASSWORD`, `QDRANT_API_KEY`, `NEO4J_PASSWORD`.
4. `data: dict` без Pydantic и отсутствие `response_model` в остатке `admin_models.py`;
   разбор `admin_models.py` на модули (2542 строки).

## Как возобновить

1. `git -C C:\VSCODE_PROJECT\kag-system-enhanced log --oneline -3` и `git status` —
   сверить с этим файлом.
2. `python scripts/scan_sync_io.py src` — текущая карта (166 мест).
3. Прочитать `docs/guides/sync-io-audit.md` — что закрыто, замеры, обоснование остатка.
4. Дальше по бэклогу, правило «один шаг»: изменение → docs/скилл → коммит/пуш.

## Инварианты (не нарушать)

- Файлы пользователя не удалять; сироты — в `data/orphans_removed`.
- Секреты не хардкодить в командные строки (только env/stdin); не печатать в чат;
  в репозитории и скиллах секретов нет — тест-страж `tests/test_no_secrets_in_repo.py`.
- Деплой: локально build → push → на 18 `git pull` → `docker-compose pull` →
  `up -d --force-recreate api` (worker с идущей обработкой не трогать) → health.
- Правки больших файлов — отдельным скриптом-файлом (без heredoc), проверка грепом
  по строкам, затем живая проверка на стенде.
- Файлы внутри project-local `.hermes/` нередактируемы для агента — не тратить на них
  попытки (этот handoff поэтому и лежит в `docs/`).

## Что дальше (открытые задачи, 2026-09-14)

Базы для сравнения: качество ответов (LLM-судья, 20 вопросов с эталонами) — перемерять В ТОМ ЖЕ
заходе, что и кандидата: тот же промпт дал **0.635** (14.09) и **0.6000** (17.09), то есть шум
±0.04 (цена одного вопроса 0.05). Разницу меньше ~0.05 считать отсутствием эффекта.
**НОВАЯ БАЗА 19.09.2026: 0.7579 по 19 вопросам (0.6316 → 0.7579 на общих: лучше 13, хуже 3) —
после мягкого домена, no_think в чате и восстановления реестра. Таблицу и разбор см. в
`docs/guides/reranker-and-answer-quality.md`, раздел «Замер 19.09.2026».**
retrieval на 7 исторических вопросах hit@1 0.7143 / hit@3 1.0 / MRR 0.8333; покрытие
сравнительных вопросов 0.875.

1. ~~Гипотеза ПОЛНОТЫ промпта~~ — **ЗАКРЫТО 17.09: эффекта нет** (0.5950 против 0.6000,
   лучше 5 / хуже 7 / без изменений 8, длина ответа +5.3%). Промпт откачен к действующему
   (2550 символов, сверен посимвольно). Промпт как рычаг исчерпан: «короче и конкретнее» — хуже
   (0.555), «полнее» — без изменений. Разбор и инструменты — `docs/guides/reranker-and-answer-quality.md`
   (раздел «A/B промпта чата №2»), `reports/_scratch/pc_stage{1,2,3}.sh`, `pc_compare.py`, `pc_weak_reason.py`.
2. Слабые ответы при факте в контексте: пентест 2-МР, ИИ на финансовом рынке, профиль защиты ПО
   (0.00) — генерация не называет факты. УТОЧНЕНО 17.09: те же вопросы в разных прогонах дают
   0.00 и 0.90 при неизменных поиске и корпусе, то есть это НЕ только генерация — вопрос в том,
   попадает ли фрагмент с фактом в отданный контекст. Поэтому начинать с диагностики контекста:
   `reports/_scratch/fact_rank.py` (ранг чанка с фактом) и доля отказов «информация не найдена»
   (в обоих прогонах A/B — 4 из 20). Дополнительные рычаги: глубина контекста top-5 вместо top-10,
   пометка лучшего фрагмента.
3. Починка сравнительного режима (пункт 4): маппинг стороны на документ брать из поиска по фрагментам,
   проверять обозначение, не сужать контекст. Включать только после улучшения относительно 0.875.
   Детали — `docs/guides/comparison-mode.md`.
4. Порог «нет ответа» в чате: контрольные вопросы дают top-1 0.56–0.65, вопрос без документа в корпусе —
   0.91 (максимум). Порог порядка 0.75–0.80 + жёсткий отказ ниже ~0.45–0.50.
5. Перезамер реранкера на большем наборе вопросов (сейчас выключен: 0.50 против 0.55–0.61 без него).
6. Корпусные пробелы: отмена Р 50.1.112—2016 с заменой на Р 1323565.1.041—2022 и макропруденциальные
   надбавки 40→100% — фактов в корпусе нет, лечится добавлением документов, а не кодом.
